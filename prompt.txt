Python 3.7互換にしてください。

現在以下のエラーが発生しています。

ImportError: cannot import name 'StrEnum' from 'enum'

要件:

- Python 3.7対応
- Python 3.11依存の機能を排除
- StrEnumは以下へ置換

from enum import Enum

class Xxx(str, Enum):
    ...

- 動作は変更しない

Python 3.7で起動できるよう修正してください。
